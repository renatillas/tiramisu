import * as $promise from "../../../gleam_javascript/gleam/javascript/promise.mjs";
import {
  new_ as new$,
  text,
  bytes,
  name,
  mime,
  size,
  createObjectURL as create_object_url,
} from "../../file_ffi.mjs";

export { bytes, create_object_url, mime, name, new$, size, text };
