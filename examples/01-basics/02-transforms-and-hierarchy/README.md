# 01-basics/02-transforms-and-hierarchy

This example makes the scene graph visible.

It demonstrates:

1. Using `tiramisu.empty` as a transform parent.
2. Composing position and rotation across nested nodes.
3. Building a scene out of multiple child primitives.
4. Thinking in scene hierarchy rather than independent objects.
