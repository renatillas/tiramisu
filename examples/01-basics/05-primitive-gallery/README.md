# 01-basics/05-primitive-gallery

This example is a visual reference for built-in geometry.

It demonstrates:

1. Box, sphere, cylinder, cone, torus, and plane primitives.
2. `hidden`, `cast_shadow`, and `receive_shadow`.
3. How to keep a capability example visual instead of purely structural.
