# 04-layout/01-multiple-renderers

This example demonstrates the renderer/scene split directly.

It demonstrates:

1. Mounting multiple independent `tiramisu.renderer` instances.
2. Giving each renderer its own scene and active camera.
3. Rendering separate worlds on the same page.
